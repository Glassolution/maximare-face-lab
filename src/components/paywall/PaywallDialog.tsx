import { PaywallContext } from '@/lib/paywall';

interface PaywallDialogProps {
  isOpen: boolean;
  onClose: () => void;
  context?: PaywallContext;
}

export const PaywallDialog = (_props: PaywallDialogProps) => {
  return null;
};
